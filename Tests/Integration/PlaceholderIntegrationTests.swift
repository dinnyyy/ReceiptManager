import XCTest

/// Placeholder so this directory (and the `ReceiptVaultTests` target's
/// source path in project.yml) exists on a fresh clone - git doesn't
/// track empty directories. Spec 17.2's integration test list (signup ->
/// workspace -> first purchase, RLS negative test via the app layer,
/// offline save -> reconnect -> synced, Item -> Warranty -> Proof Pack)
/// is not implemented yet; see CLAUDE.md's Quality checklist.
final class PlaceholderIntegrationTests: XCTestCase {
    func testPlaceholder() throws {
        XCTAssertTrue(true)
    }
}
