import XCTest

/// Placeholder so this directory (and the `ReceiptVaultUITests` target's
/// source path in project.yml) exists on a fresh clone - git doesn't
/// track empty directories. Spec 17.3's UI happy-path test (fresh install
/// -> sign in -> scan -> review -> save -> search -> Proof Pack) is not
/// implemented yet; see CLAUDE.md's Quality checklist.
final class PlaceholderUITests: XCTestCase {
    func testPlaceholder() throws {
        XCTAssertTrue(true)
    }
}
